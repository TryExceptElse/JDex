package io.github.tryexceptelse.jdex.tests;

import static org.junit.Assert.assertTrue;

import org.junit.Test;


/**
 * Simply test that project is able to be built correctly.
 */
public class BuildTest {

 /**
  * As a placeholder, this test should simply return true if the build
  * process gets far enough to run it. More advanced tests may follow
  */
  @Test
  public void buildTest() {
    assertTrue(true);
  }
}
